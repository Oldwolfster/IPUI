# Mouse.py  Mouse button constants for IPUI.
# Use with ip.mouse_down(Mouse.LEFT), ip.mouse_pressed(Mouse.RIGHT), etc.


class Mouse:
    """Mouse button constants — index into pygame.mouse.get_pressed(3)."""

    LEFT            = 0
    MIDDLE          = 1
    RIGHT           = 2
